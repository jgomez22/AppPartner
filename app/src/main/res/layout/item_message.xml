<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
                name="viewModel"
                type="com.mobile.apppartner.ui.message.MessageViewModel" />
    </data>

    <android.support.constraint.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="8dp">

        <TextView
                android:id="@+id/txtOtherMessage"
                app:mutableText="@{viewModel.getMessageValue()}"
                android:background="@drawable/other_message_bubble"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxWidth="240dp"
                android:padding="15dp"
                android:elevation="5dp"
                android:textColor="#ffffff"
                tools:ignore="MissingConstraints" tools:layout_editor_absoluteY="8dp"
                tools:layout_editor_absoluteX="0dp"/>

        <TextView
                android:id="@+id/txtOtherMessageTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="10sp"
                android:textStyle="bold"
                app:mutableText="@{viewModel.getMessageValue()}"
                app:layout_constraintBottom_toBottomOf="@+id/txtOtherMessage" tools:ignore="MissingConstraints"
                tools:layout_editor_absoluteX="109dp"/>

    </android.support.constraint.ConstraintLayout>


</layout>